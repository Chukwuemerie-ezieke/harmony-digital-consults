// Form submission endpoint — Apps Script web app URL.
// Until set, forms fall back to opening the user's email client (mailto).
window.HARMONY_CONFIG = {
  formEndpoint: "" // <-- paste your Apps Script web app /exec URL here once deployed
};
